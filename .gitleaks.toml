title = "api-scaffold-federated-rodit-auth gitleaks config"

[allowlist]
description = "Documented non-secret placeholders in committed config"
regexes = [
  '''HMAC-session-secret-is-not-set''',
  '''api-url-not-set\.example\.com''',
]
